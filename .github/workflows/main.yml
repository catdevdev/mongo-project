name: Run Date Command on Self-Hosted Runner

on:
  workflow_dispatch: # Allows you to manually trigger this workflow

jobs:
  show-date:
    runs-on: self-hosted # Use your self-hosted runner

    steps:
      - name: Check Date
        run: date # Executes the date command
